# 麻将规则备忘录（2026-04-09 重要纠正）

## 核心规则（K哥铁律，2026-04-09 确认）

### 胡/碰/出牌优先级
**胡 > 碰 > 出牌**，流程如下：

```
A出牌
  → B能胡 → 直接胡（不做碰）
  → B不能胡，但能碰 → 选择碰 → 碰后立即出牌（不能再摸牌！）
  → B不能碰 → 出牌
```

### 碰后规则（纠正）
- **碰后不能摸牌！** 碰完直接出一张牌
- **碰后不能胡！** 碰了就不能再选择胡
- anKong（暗杠/加杠）在碰后可以检查，但碰本身没有摸牌

### 杠后规则（确认）
- **暗杠**：必须补摸一张牌，然后检查自摸
- **加杠**：必须补摸一张牌，然后检查自摸
- **杠他人第四张牌**：必须补摸一张（特殊杠，与普通碰不同）

### 自摸检查时机
- 每次正常摸牌后检查自摸
- 杠后补摸检查自摸
- **碰后不检查自摸**（碰不补摸）

### 无花自摸（门前清一色）
- 条件：`isSelfDrawn=true` + 门前（exposedMelds）无：花、风刻、**箭刻**、**杠牌**
- 适用牌型：碰碰胡、混一色
- **注**：门前暗杠同样破坏无花自摸资格

### 手牌数约束
- 摸牌后：14/11/8/5/2 张
- 碰后：11/8/5/2 张（净减3张，不补摸）
- 杠后：10/7/4/1 张（暗杠4张，补1摸=净3+1=4已出）

---

## 待修复 Bug（2026-04-09 发现）

### Bug 1: menqingKeepBonus 过高（已部分修复）
- **位置**: train-baseline.ts DEFAULT_POLICY
- **问题**: menqingKeepBonus=0.0 导致 GA 搜索到 2.8，Bots 死守 menqing 路线导致普通胡牌率崩溃
- **修复**: 初始值改为 0.8，搜索上限改为 3.0（commit `59f5295`）
- **状态**: 正在 10×1000 局测试中

### Bug 2: 无花自摸缺失箭刻和杠牌检查
- **位置**: scoring.ts `hasWindMelds()`
- **问题**: 无花自摸的"门前无拦"只检查了花，实际还应检查**箭刻**和**门口杠牌**
- **修复**: `hasWindMelds` 扩展检查风刻/箭刻/明杠/暗杠/加杠（commit `9d84ab6`）
- **另**: `applyAnKong` 推 `CONCEALED_KONG` 而非 `KONG`，与 `hasWindMelds` 对齐（commit `726348d`）

---

## 当前训练状态（2026-04-10）

- 目标：胡牌率≥90%，门清率 7-12%
- drawPenalty 加倍（50000→100000）惩罚流局（commit `c480ab6`）
- 每回合快照 + testOneGame 单局调试工具（commit `c480ab6`）
- 无花自摸完整化 + 暗杠类型修复

---

## 2026-04-10 当晚新增结论（K哥要求）

### A. 流局也必须输出回合明细（已实现）
- **结论**：单局测试不再依赖“必须有人胡牌”才能写 round 文件。
- **实现**：`scripts/train-baseline.ts` 在流局时也返回结果并写入 `training-output/test/round-*.md`。
- **提交**：`ea60fcf`

### B. 暗杠 UI 规则落地（已实现）
- **规则**：暗杠仍在副露区，与其他副露并排；但暗杠牌面显示为牌背。
- **实现文件**：
  - `app/components/PlayerSelfArea.vue`
  - `app/components/PlayerOtherArea.vue`
- **提交**：`8b1d40e`

### C. 无花判定口径（再次确认）
- **普通花算花**：会进入门口花/花牌统计。
- **百搭花不算花**：当本局百搭是花牌组（春夏秋冬或梅兰竹菊）时，按百搭逻辑处理，不按普通花拦截无花口径。
- **对应代码**：`server/utils/scoring.ts`（`isWildFlower` 与 `hasNoFlowers` 判定分支）

### D. 当晚训练/调试关键经验
- `runGame` 在多处摸牌为空时直接 `return null`，会导致调试流程中断；已改为流局结果输出用于分析。
- 100% 流局问题排查要优先检查：
  1) 百搭分布是否落空（4家起手无百搭）
  2) 早期机械丢牌（mechanicalDiscard）是否破坏成型路线
  3) 胡牌检查触发路径与牌型识别是否一致

### E. 次日优先待办
1. 用 `qwencode/qwen35` 分批做全量 review（规则/训练/UI 三段）
2. 补齐 `train-ai-ak.ts` 抢杠胡资格检查（与 gameManager 对齐）
3. 继续优化前3回合丢牌策略，降低“早期拆搭子”
4. 对“无花判定”增加可读日志（普通花数/百搭花数分开显示）
