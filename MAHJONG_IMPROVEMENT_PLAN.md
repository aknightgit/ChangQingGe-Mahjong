# 麻将项目优化计划

> **更新日期**：2026-04-23
> **维护状态**：进行中
> **说明**：本计划只保留未完成事项，已完成项不再重复记录。

---

## 当前判断

- 主训练脚本 `scripts/train-ai-ak.ts` 的参数入口、执行链路、日志输出已经打通。
- 训练日志现已统一落到项目目录 `training-output/`，每次运行前清空旧结果，但会保留 `training-output/save/`。
- 牌局主流程的显性卡死点已经修过一轮，但“玩家某些场景下可能连续打两次牌”仍需深入定位，不能仅以前端拦截视为彻底解决。
- AI-AK 当前的核心问题仍然不是 wrapper，而是训练质量差，胡牌率长期锁死在低位甚至 0。

---

## P0 本周优先项

### P0-1 玩家连续打两次牌深度排查

目标：彻底确认“玩家打完牌后没有 move 到下家，自己还能继续打”的根因，并补上可回归验证。

待做：
- 复盘 `discard -> checkPendingActions -> pass/claim -> moveToNextPlayer` 的完整状态机链路。
- 针对真人手动操作补日志，明确记录 `currentPlayerIndex`、`pendingActions`、`availableActions`、玩家暗手张数。
- 排查是否存在服务端已切人、前端状态未刷新，或服务端本身未切人的两类分叉。
- 给“玩家出牌后仍可继续出牌”补一条专门回归测试，覆盖 `peng/chow/gang` 竞争、bot `pass`、网络轮询延迟三类场景。
- 若再次复现，优先以服务端为准收紧 `DISCARD` 发放条件，而不是继续堆前端防守。

完成标准：
- 真人手测至少 1 局不再出现该问题。
- 新增回归测试稳定通过。

### P0-2 训练收敛失败定位

目标：找出 AI-AK 长期不进听、不胡牌的真实坏点。

待做：
- 继续跟踪 `aiDiscard()` 在“进入死手前 2 到 4 步”的评分路径。
- 抽样记录“候选弃牌评分榜、真实选择、成听机会、错过听口”。
- 区分三类问题：策略主动打散、局面天然无听、规则链路误判。
- 若仍大量出现 `无听口`，继续检查中盘推进规则是否把手牌带进不可收敛残局。
- 建立一组最小训练基线：`1x2`、`1x20`、`1x100`，固定输出听牌次数、可胡机会、流局率。

完成标准：
- 至少明确一个可稳定复现的坏路径。
- 能用日志或测试证明坏点属于“策略”还是“规则”。

### P0-3 训练产物与流程规范化

目标：保证任意环境下训练都只保留最后一次结果，便于对比。

待做：
- 保持 `training-output/` 作为唯一默认输出目录。
- 保持每次训练前自动清理旧日志，但永远跳过 `training-output/save/`。
- 后续新增训练脚本时，统一复用同一套 `prepareTrainingOutputDir()` 逻辑。
- 补一条脚本级回归检查，确认清理逻辑不会误删 `save/`。

完成标准：
- `train-ai-ak`、`train-baseline`、`run-training` 三条链一致。
- `save/` 中的文件在重复训练后仍然保留。

---

## P1 次优先项

### P1-1 回合状态机稳固

待做：
- 继续检查 `claim`、`pass`、`timeout`、`bot auto action` 的衔接顺序。
- 核对 `pendingActions` 清空后的唯一推进出口，避免多处分散推进。
- 对 “Skipped: pending actions remaining” 相关日志做降噪和上下文增强，便于下次直接定位。

### P1-3 AI 规则引擎回归覆盖

待做：
- 继续补残局判胡、听牌枚举、百搭特殊型的隔离测试。
- 把训练中抽到的真实异常牌型沉淀成固定回归样本。

---

## 验收指标

- 玩家手动对局中不再出现“同一回合连续打两次牌”。
- 牌局流程在 bot `pass`、多人竞争动作、超时动作后不再卡死。
- AI-AK 训练最小样本不再是稳定 `0 胡 / 全流局`，或者至少已明确根因并有对应修复项。
- 训练目录始终只保留最后一次结果，`training-output/save/` 不受影响。

---

## 建议执行顺序

1. 先做 `P0-1`，把“玩家连续打两次牌”彻底验死。
2. 再做 `P0-2`，继续收敛 AI-AK 训练坏点。
3. 然后补 `P0-3` 的回归检查，固定训练产物规则。
4. 最后推进 `P1` 项，补强体验和测试覆盖。
