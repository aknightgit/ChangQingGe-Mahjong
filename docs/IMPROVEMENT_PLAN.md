# 麻将项目改进计划

> 更新日期：2026-04-24
> 执行原则：现阶段以 `AI-AK` 做牌收敛和胡牌能力优化为主，既有 bug 先转入观察，不抢主线。

---

## 一、当前主线

### [TODO] Train-001：AI-AK 尽快学会做牌、进听、胡牌
**优先级**：P0

**配套设计**
- 详细执行方案见 [AI_AK_ROUTE_EXECUTION_PLAN.md](E:\MyWorkSpace\ChangQingGe-Mahjong\docs\AI_AK_ROUTE_EXECUTION_PLAN.md)

**现状**
- 主训练入口、参数传递、输出目录、报告生成已基本理顺。
- 近期已经出现过至少一局真实胡牌，说明主流程不是完全堵死。
- 但整体训练结果仍然偏差很大，`AI-AK` 还没有稳定形成“中盘收敛 -> 进听 -> 可胡”的正循环。

**下一步**
1. 优先优化 `aiDiscard()` 的中盘路线，让 AI 先学会留搭子、做顺、快速收口。
2. 给弃牌评分增加更直接的“前视进张”判断，而不是只靠静态保留分。
3. 每次只做一轮最小训练验证，重点看 `听牌进入次数 / 可胡机会 / 流局率` 是否改善。

---

### [TODO] Train-002：继续收敛 AI-AK 的牌型路线权重
**优先级**：P1

**现状**
- 当前策略仍然偏早拆门、偏碰碰/染手，容易把中盘打散。
- 部分路线权重更像“冲大牌”，不适合现在这种连基本胡牌都不稳定的阶段。

**下一步**
1. 降低过早做清一色/碰碰胡的激进度。
2. 提高对子、连张、两面、嵌张的保留权重。
3. 等胡牌率恢复到可用区间后，再重新增加大牌路线权重。

---

### [TODO] Train-003：自摸率与多人胡分布在训练恢复后再做二次优化
**优先级**：P2

**现状**
- 自摸率、多人胡牌率、放冲展示等链路目前仍有观察项，但不是当前阻塞主线。
- 这些指标应该放在“能稳定胡牌”之后再做二次修整。

**下一步**
1. 先以“能胡牌、少流局、能进听”为第一目标。
2. 训练主线稳定后，再回头优化自摸率和多人胡分布。

---

## 二、观察项

### [OBSERVE] Bug-001：玩家异常连续出两次牌
- 前后端护栏和回归已经补上。
- 最近手动实测已成功完整跑过一局，并出现首次胡牌，暂未再次复现。
- 先保留观察，不作为当前阻塞项；若再次出现，再回到 `pendingActions / canDiscard / 轮询同步` 链路深挖。

### [OBSERVE] Bug-002：训练/报告中的多人胡牌率长期显示 0%
- 当前仍未用新的真实多人胡样本做最终验真。
- 先保留观察，等训练主线恢复后再用真实样本回收这条。

### [OBSERVE] Bug-003：放冲牌与实际手牌展示链路
- 训练侧记录方式已经改过。
- 目前缺的是新样本验收，不是明确阻塞 bug。

### [OBSERVE] Bug-004：Bot/训练链路的非法手数状态
- 诊断日志已经接好。
- 后续只在 smoke 或最小训练时顺手检查，有新告警再单独立项。

---

## 三、已完成并从计划中移除的事项

以下事项已完成，不再继续占用主计划：
- 训练输出目录统一到项目内 `training-output/`，并保留 `training-output/save/`
- 训练报告中的骰子倍率/继承倍率/全局倍率 fallback
- 平均回合、平均总筹码、detail-round 标题、全局最大赢局统计修正
- 胡牌牌型分布聚合所有赢家
- 放冲牌按 `tile.id` 精确剥离
- 训练脚本 `INV_TRACE` 的 `DEAL/DRAW/DISC/CLAIM` 手数期望值统一
- `pendingActions` 卡死主链路修复
- 建房双击、吃碰杠胡按钮提示、弃牌区位置微调
- `P1-2` 操作提示与桌面反馈收口：抢动作提示条、移动端具体操作提示、按钮呼吸提示统一
This file has been intentionally cleared.

Use `AI_policies/AI-Policy-guide.md` as the canonical guide for future AI policy iteration.
